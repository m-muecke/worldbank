% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projects.R
\name{wb_project}
\alias{wb_project}
\title{World Bank project data}
\source{
\url{https://search.worldbank.org/api/v2/projects}
}
\usage{
wb_project(
  id = NULL,
  country = NULL,
  status = NULL,
  region = NULL,
  search = NULL,
  start_date = NULL,
  end_date = NULL
)
}
\arguments{
\item{id}{(\code{NULL} | \code{character(1)})\cr
Project ID to query, e.g. \code{"P163868"}. Default \code{NULL}.
If provided, other filters are ignored.}

\item{country}{(\code{NULL} | \code{character()})\cr
ISO country code(s) to filter by, e.g. \code{"BR"} or \code{c("BR", "IN")}. Default \code{NULL}.}

\item{status}{(\code{NULL} | \code{character(1)})\cr
Project status to filter by. One of \code{"Active"}, \code{"Closed"}, \code{"Dropped"}, or \code{"Pipeline"}.
Default \code{NULL}.}

\item{region}{(\code{NULL} | \code{character(1)})\cr
Region name to filter by, e.g. \code{"South Asia"}. Default \code{NULL}.}

\item{search}{(\code{NULL} | \code{character(1)})\cr
Free-text search term. Default \code{NULL}.}

\item{start_date}{(\code{NULL} | \code{character(1)})\cr
Board approval start date in \code{"YYYY-MM-DD"} format. Default \code{NULL}.}

\item{end_date}{(\code{NULL} | \code{character(1)})\cr
Board approval end date in \code{"YYYY-MM-DD"} format. Default \code{NULL}.}
}
\value{
A \code{data.frame()} with World Bank project data. The columns are:
\item{id}{The project ID.}
\item{project_name}{The project name.}
\item{status}{The project status.}
\item{approval_date}{The board approval date.}
\item{closing_date}{The closing date.}
\item{country_code}{The ISO country code.}
\item{country}{The country name.}
\item{region}{The region name.}
\item{total_commitment}{The total commitment amount in millions USD.}
\item{ibrd_commitment}{The IBRD commitment amount in millions USD.}
\item{ida_commitment}{The IDA commitment amount in millions USD.}
\item{lending_instrument}{The lending instrument type.}
\item{borrower}{The borrower name.}
\item{implementing_agency}{The implementing agency name.}
\item{url}{The project URL.}
}
\description{
Query World Bank project data from the Projects API.
}
\examples{
\dontshow{if (httr2::is_online()) withAutoprint(\{ # examplesIf}
\donttest{
# active projects in Brazil related to education
wb_project(country = "BR", status = "Active", search = "education")

# look up a specific project
wb_project(id = "P163868")
}
\dontshow{\}) # examplesIf}
}
\concept{projects data}
