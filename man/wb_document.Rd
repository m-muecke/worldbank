% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documents.R
\name{wb_document}
\alias{wb_document}
\title{World Bank document data}
\source{
\url{https://search.worldbank.org/api/v3/wds}
}
\usage{
wb_document(
  search = NULL,
  country = NULL,
  type = NULL,
  project = NULL,
  start_date = NULL,
  end_date = NULL,
  id = NULL,
  limit = NULL
)
}
\arguments{
\item{search}{(\code{NULL} | \code{character(1)})\cr
Free-text search term. Default \code{NULL}.}

\item{country}{(\code{NULL} | \code{character()})\cr
Two-character World Bank country code(s) to filter by, e.g. \code{"BR"} or \code{c("BR", "IN")}.
Matching is case insensitive, and documents for any of the given codes are returned.
Default \code{NULL}.}

\item{type}{(\code{NULL} | \code{character()})\cr
Document type(s) to filter by, e.g. \code{"Report"} or \code{"Project Appraisal Document"}. Documents
of any of the given types are returned. Default \code{NULL}.}

\item{project}{(\code{NULL} | \code{character()})\cr
Project ID(s) to filter by, e.g. \code{"P180429"}. Documents for any of the given projects are
returned. Default \code{NULL}.}

\item{start_date}{(\code{NULL} | \code{character(1)})\cr
Document start date in \code{"YYYY-MM-DD"} format. Default \code{NULL}.}

\item{end_date}{(\code{NULL} | \code{character(1)})\cr
Document end date in \code{"YYYY-MM-DD"} format. Default \code{NULL}.}

\item{id}{(\code{NULL} | \code{character()})\cr
Document ID(s) to filter by, as returned in the \code{id} column. Default \code{NULL}.}

\item{limit}{(\code{NULL} | \code{integer(1)})\cr
The maximum number of documents to return. Default \code{NULL}. If \code{NULL}, all matching documents
are returned, which can take many requests for broad queries.}
}
\value{
A \code{data.frame()} with World Bank document data. The columns are:
\itemize{
\item \code{id}: The document ID.
\item \code{title}: The document title.
\item \code{type}: The document type.
\item \code{major_type}: The major document type.
\item \code{country_code}: The ISO country code.
\item \code{country}: The country name.
\item \code{region}: The administrative region name.
\item \code{language}: The document language.
\item \code{date}: The document date.
\item \code{report_number}: The report number.
\item \code{project_id}: The ID of the associated project, or \code{NA} if there is none.
\item \code{url}: The document URL.
\item \code{pdf_url}: The PDF URL.
\item \code{abstract}: The abstract.
}
}
\description{
Search the World Bank Documents & Reports archive from the Documents & Reports API.
}
\examples{
\dontshow{if (httr2::is_online()) withAutoprint(\{ # examplesIf}
\donttest{
# project appraisal documents for Brazil published in 2024
wb_document(
  country = "BR",
  type = "Project Appraisal Document",
  start_date = "2024-01-01",
  end_date = "2024-12-31"
)

# all documents belonging to a project
wb_document(project = "P180429")

# the first 100 documents mentioning climate
wb_document(search = "climate", limit = 100)
}
\dontshow{\}) # examplesIf}
}
\concept{documents data}
